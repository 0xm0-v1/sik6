# .github/workflows/ci-bootstrap.yml
name: CI Bootstrap

on:
  pull_request:
    branches: [ "main" ]
  push:
    branches: [ "main" ]
  workflow_dispatch: {}

jobs:
  static-checks:
    name: Static Checks (placeholder)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Placeholder
        run: echo "Static checks placeholder — format/lint in verify-only mode will be added later"

  build:
    name: Build (placeholder)
    runs-on: ubuntu-latest
    needs: static-checks
    steps:
      - name: Placeholder
        run: echo "Build placeholder — to be implemented in a later story"

  test:
    name: Test (placeholder)
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Placeholder
        run: echo "Test placeholder — to be implemented later (coverage, etc.)"

  deploy:
    name: Deploy (placeholder)
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Placeholder
        run: echo "Deploy placeholder — to be implemented later (registry/envs/sandbox)"
